% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tokenizers.R
\name{make_wordpiece_tokenizer}
\alias{make_wordpiece_tokenizer}
\title{Create a WordPiece tokenizer for BERT models that lack \code{tokenizer.json}}
\usage{
make_wordpiece_tokenizer(vocab_path, do_lower_case = NULL, max_length = 512L)
}
\arguments{
\item{vocab_path}{Path to the model's \code{vocab.txt} file.}

\item{do_lower_case}{Logical, whether to lowercase input before
tokenization.  If \code{NULL} (the default), inferred from the vocabulary:
if no tokens start with an uppercase letter, the vocab is treated as
uncased.  Pass an explicit value if \code{tokenizer_config.json} specifies it.}

\item{max_length}{Default maximum sequence length, including the two
special tokens \verb{[CLS]} and \verb{[SEP]}.  Can be overridden later via
\code{tokenizer$enable_truncation()}.}
}
\value{
A list with methods \code{encode_batch(texts)}, \code{enable_padding()},
and \code{enable_truncation(max_length)}.
}
\description{
Older BERT-family models (SciBERT, BioBERT, the original BERT-base, etc.)
ship a \code{vocab.txt} file but not the HuggingFace fast-tokenizer format.
This function builds a tokenizer object exposing the same methods as
\code{tok::tokenizer$from_pretrained()} (\code{encode_batch}, \code{enable_padding},
\code{enable_truncation}) so it can be used interchangeably with
\code{\link[=embed_texts]{embed_texts()}}.
}
\details{
Uses the CRAN package \code{wordpiece} for the WordPiece algorithm itself.
}
\examples{
\dontrun{
  vocab <- hfhub::hub_download("allenai/scibert_scivocab_cased", "vocab.txt")
  tk <- make_wordpiece_tokenizer(vocab)
  tk$encode_batch(c("This is a sentence.", "Another one."))
}
}
