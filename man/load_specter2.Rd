% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_model.R
\name{load_specter2}
\alias{load_specter2}
\title{Load a SPECTER2 model with a task-specific adapter}
\usage{
load_specter2(
  adapter = "allenai/specter2",
  base = "allenai/specter2_base",
  adapter_name = "[PRX]"
)
}
\arguments{
\item{adapter}{HuggingFace repo ID of the adapter checkpoint.  Must contain
a \code{pytorch_adapter.bin} file and an \code{adapter_config.json}.
Default: \code{"allenai/specter2"} (proximity adapter).}

\item{base}{HuggingFace repo ID of the base model.
Default: \code{"allenai/specter2_base"}.}

\item{adapter_name}{Name of the adapter as stored in the checkpoint's weight
keys.  For all official SPECTER2 adapters this is \code{"[PRX]"}.}
}
\value{
A \code{bert_encoder} object (same class as \code{\link[=load_hf_bert]{load_hf_bert()}}) with
Pfeiffer adapter modules injected into every transformer layer.  The list
also carries \verb{$adapter_repo} recording which adapter was loaded.
}
\description{
SPECTER2 \doi{10.48550/arXiv.2211.13308} extends the original SPECTER model
with task-specific Pfeiffer adapters trained on millions of citation pairs.
This function loads the base encoder from \code{allenai/specter2_base} and
injects the chosen adapter into each transformer layer, matching the
behaviour of the Python \code{adapters} library.
}
\details{
Available adapters on the HuggingFace Hub:
\describe{
\item{\code{"allenai/specter2"}}{Proximity / similarity — recommended for
document retrieval and topic modeling (default).}
\item{\code{"allenai/specter2_adhoc_query"}}{Query-side adapter for asymmetric
retrieval (query vs. document).}
\item{\code{"allenai/specter2_classification"}}{Trained for paper
classification tasks.}
}

The returned object is a standard \code{bert_encoder} compatible with
\code{\link[=embed_texts]{embed_texts()}}, \code{\link[=embed_texts_cached]{embed_texts_cached()}}, and \code{\link[=fit_bertopic]{fit_bertopic()}}.
}
\examples{
\dontrun{
  enc <- load_specter2()   # proximity adapter — best for topic modeling
  emb <- embed_texts(enc, c("Graph neural networks for drug discovery.",
                             "Climate tipping points and carbon budgets."))
}
}
