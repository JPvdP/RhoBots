% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_embedders.R
\name{load_openai_embedder}
\alias{load_openai_embedder}
\title{Load an OpenAI embedding model}
\usage{
load_openai_embedder(
  model = "text-embedding-3-small",
  api_key = Sys.getenv("OPENAI_API_KEY"),
  dimensions = NULL,
  base_url = "https://api.openai.com/v1"
)
}
\arguments{
\item{model}{OpenAI embedding model name.
\itemize{
\item \code{"text-embedding-3-small"} (default)  --  1536-d, fast and cheap
\item \code{"text-embedding-3-large"}  --  3072-d, highest quality
\item \code{"text-embedding-ada-002"}  --  legacy 1536-d model
}}

\item{api_key}{API key.  Defaults to \code{Sys.getenv("OPENAI_API_KEY")}.}

\item{dimensions}{Optional integer to request a reduced output dimension
(only supported by \verb{text-embedding-3-*} models).}

\item{base_url}{API base URL.  Override for Azure OpenAI or API proxies.}
}
\value{
An \code{api_embedder} object.
}
\description{
Returns an \code{api_embedder} that calls the OpenAI Embeddings API.
Requires the \pkg{httr2} package and a valid OpenAI API key.
}
\details{
The returned object is compatible with \code{\link[=embed_texts]{embed_texts()}},
\code{\link[=embed_texts_cached]{embed_texts_cached()}}, and \code{\link[=fit_bertopic]{fit_bertopic()}}.
}
\examples{
\dontrun{
  enc <- load_openai_embedder()   # uses OPENAI_API_KEY env var
  emb <- embed_texts(enc, c("Transformers in R.", "Topic modelling."))
  fit <- fit_bertopic(enc, docs = abstracts)
}
}
