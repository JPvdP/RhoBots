% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_embedders.R
\name{load_cohere_embedder}
\alias{load_cohere_embedder}
\title{Load a Cohere embedding model}
\usage{
load_cohere_embedder(
  model = "embed-english-v3.0",
  api_key = Sys.getenv("COHERE_API_KEY"),
  input_type = "search_document"
)
}
\arguments{
\item{model}{Cohere embedding model name.
\itemize{
\item \code{"embed-english-v3.0"} (default) — 1024-d English
\item \code{"embed-multilingual-v3.0"} — 1024-d, 100+ languages
\item \code{"embed-english-light-v3.0"} — 384-d, faster/cheaper
}}

\item{api_key}{API key.  Defaults to \code{Sys.getenv("COHERE_API_KEY")}.}

\item{input_type}{Cohere input type controlling the embedding space used.
\itemize{
\item \code{"search_document"} (default) — for indexing documents
\item \code{"search_query"} — for embedding queries
\item \code{"clustering"} — optimized for clustering
\item \code{"classification"} — optimized for classification
}}
}
\value{
An \code{api_embedder} object.
}
\description{
Returns an \code{api_embedder} that calls the Cohere Embed API.
Requires the \pkg{httr2} package and a valid Cohere API key.
}
\details{
The returned object is compatible with \code{\link[=embed_texts]{embed_texts()}},
\code{\link[=embed_texts_cached]{embed_texts_cached()}}, and \code{\link[=fit_bertopic]{fit_bertopic()}}.
}
\examples{
\dontrun{
  enc <- load_cohere_embedder()   # uses COHERE_API_KEY env var
  emb <- embed_texts(enc, docs)
}
}
