% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy.R
\name{hierarchical_topics}
\alias{hierarchical_topics}
\title{Build a hierarchical topic tree from a fitted topic model}
\usage{
hierarchical_topics(fit, method = "ward.D2", top_n_terms = 10L)
}
\arguments{
\item{fit}{A \code{bertopic_fit} object from \code{\link[=fit_bertopic]{fit_bertopic()}}.}

\item{method}{Linkage method passed to \code{\link[stats:hclust]{stats::hclust()}}.  \code{"ward.D2"}
(default) tends to produce compact, balanced clusters.  Other good
choices: \code{"average"} (UPGMA), \code{"complete"}.}

\item{top_n_terms}{Number of top terms to record at each internal node.}
}
\value{
A list of class \code{hierarchical_topics} with elements:
\describe{
\item{\code{hclust}}{the \code{hclust} object (for use with base R dendrogram
functions if desired)}
\item{\code{merge_df}}{a data frame with one row per merge, recording
\code{parent_id}, \code{child_left}, \code{child_right}, \code{distance} (cosine),
\code{topics} (comma-separated topic IDs), and \code{terms}}
\item{\code{topic_ids}}{integer vector of leaf topic IDs in original order}
\item{\code{method}}{the linkage method used}
}
}
\description{
Performs agglomerative clustering on the L2-normalised topic centroids
stored in \code{fit$topic_centroids} using cosine distance.  At each merge
node the function pools the c-TF-IDF scores of all constituent topics and
records the top terms, so every level of the hierarchy is labelled.
}
\details{
The result can be visualised with \code{\link[=visualize_hierarchy]{visualize_hierarchy()}}.
}
\examples{
\dontrun{
  enc <- load_hf_bert("sentence-transformers/all-MiniLM-L6-v2")
  fit <- fit_bertopic(enc, docs = abstracts)
  h   <- hierarchical_topics(fit)
  print(h)
  visualize_hierarchy(h, fit = fit)
}
}
