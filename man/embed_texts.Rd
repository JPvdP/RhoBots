% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_embedders.R, R/embed.R
\name{embed_texts.api_embedder}
\alias{embed_texts.api_embedder}
\alias{embed_texts}
\alias{embed_texts.bert_encoder}
\title{Embed a vector of texts to a numeric matrix}
\usage{
\method{embed_texts}{api_embedder}(
  encoder,
  texts,
  batch_size = NULL,
  normalize = TRUE,
  prefix = NULL,
  verbose = interactive(),
  ...
)

embed_texts(encoder, texts, ...)

\method{embed_texts}{bert_encoder}(
  encoder,
  texts,
  batch_size = 32L,
  max_length = 256L,
  normalize = TRUE,
  device = "cpu",
  prefix = NULL,
  chunk_strategy = c("truncate", "mean", "first"),
  chunk_overlap = 0L,
  verbose = interactive()
)
}
\arguments{
\item{encoder}{A loaded encoder: a \code{bert_encoder} from \code{\link[=load_hf_bert]{load_hf_bert()}} /
\code{\link[=load_specter2]{load_specter2()}}, or an \code{api_embedder} from \code{\link[=load_openai_embedder]{load_openai_embedder()}} /
\code{\link[=load_cohere_embedder]{load_cohere_embedder()}}.}

\item{texts}{A character vector of strings to embed.}

\item{batch_size}{Number of texts (or chunks) per forward pass.}

\item{normalize}{If \code{TRUE} (default), L2-normalize each row so cosine
similarity equals dot product.}

\item{prefix}{String prepended to every text before tokenization.
\code{NULL} (default) falls back to \code{encoder$prefix}; \code{""} disables prefixing
even when the encoder has a stored prefix.}

\item{verbose}{If \code{TRUE}, prints batch progress.}

\item{max_length}{Truncate/chunk token sequences to this length (including
special tokens).  Capped at the model's \code{max_position_embeddings}.}

\item{device}{Either \code{"cpu"} (default) or \code{"cuda"}.}

\item{chunk_strategy}{One of \code{"truncate"} (default), \code{"mean"}, or
\code{"first"}.  See Details. Ignored for \code{api_embedder}.}

\item{chunk_overlap}{Number of token overlap between consecutive windows
when \code{chunk_strategy != "truncate"}. Default 0.}
}
\value{
A numeric matrix with \code{length(texts)} rows and \code{hidden_size} cols.
}
\description{
S3 generic dispatching on the encoder class.  For local BERT-family models
(class \code{bert_encoder}) see the Details section.  For API-backed models
(class \code{api_embedder}) the function calls the provider's REST endpoint and
parameters \code{max_length}, \code{device}, \code{chunk_strategy}, and \code{chunk_overlap}
are ignored.
}
\details{
Tokenizes, runs the encoder forward pass, pools over tokens (mean or CLS,
auto-detected from the encoder's pooling configuration), and optionally
L2-normalizes the result.  Batches inputs for memory efficiency.

An instruction \strong{prefix} can be prepended to every text before
tokenization.  This is required for best performance with BGE and E5
models:
\itemize{
\item BGE (\verb{BAAI/bge-*}): \code{prefix = "Represent this sentence: "}
\item E5 (\verb{intfloat/e5-*}): \code{prefix = "passage: "}
}
If the encoder was loaded with a prefix via \code{\link[=load_hf_bert]{load_hf_bert()}}'s \code{prefix}
argument, that value is used automatically and need not be repeated here.
Passing \code{prefix} explicitly always takes precedence.

\strong{Long-document chunking} (\code{chunk_strategy}): BERT-family models have a
fixed maximum sequence length (usually 512 tokens) and silently truncate
longer inputs.  Setting \code{chunk_strategy = "mean"} (or \code{"first"}) instead
splits each over-length text into overlapping windows of \code{max_length}
tokens, embeds each window, and aggregates:
\itemize{
\item \code{"truncate"} (default): truncate at \code{max_length}, fast, no overhead.
\item \code{"mean"}: embed all windows, average their vectors (then normalize
if \code{normalize = TRUE}). Best quality for long documents.
\item \code{"first"}: embed only the first window. Good when the lead of a
document (abstract, summary) is the most informative part.
}
Use \code{chunk_overlap} to set the number of overlapping tokens between
consecutive windows (default 0).
}
\examples{
\dontrun{
  # General model — no prefix needed
  enc <- load_hf_bert("sentence-transformers/all-MiniLM-L6-v2")
  emb <- embed_texts(enc, c("First sentence.", "Second sentence."))

  # BGE model — set prefix at load time (applied automatically)
  enc <- load_hf_bert("BAAI/bge-base-en-v1.5",
                       prefix = "Represent this sentence: ")
  emb <- embed_texts(enc, docs)

  # E5 model — passage prefix for documents, query prefix for queries
  enc <- load_hf_bert("intfloat/e5-base-v2", prefix = "passage: ")
  doc_emb   <- embed_texts(enc, docs)
  query_emb <- embed_texts(enc, queries, prefix = "query: ")

  # Long documents: chunk and mean-pool windows
  enc <- load_hf_bert("sentence-transformers/all-mpnet-base-v2")
  emb <- embed_texts(enc, long_papers, chunk_strategy = "mean",
                      chunk_overlap = 32L)

  # API-based (no torch required)
  enc <- load_openai_embedder()
  emb <- embed_texts(enc, docs)
}
}
