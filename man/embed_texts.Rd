% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{embed_texts}
\alias{embed_texts}
\title{Embed a vector of texts to a numeric matrix}
\usage{
embed_texts(
  encoder,
  texts,
  batch_size = 32,
  max_length = 256,
  normalize = TRUE,
  device = "cpu",
  verbose = interactive()
)
}
\arguments{
\item{encoder}{A loaded encoder, as returned by \code{\link[=load_hf_bert]{load_hf_bert()}}.}

\item{texts}{A character vector of strings to embed.}

\item{batch_size}{Number of texts to tokenize and forward together.
Larger uses more memory but runs faster.}

\item{max_length}{Truncate token sequences to this length (including
special tokens like \verb{[CLS]} and \verb{[SEP]}).  Capped at the model's
\code{max_position_embeddings}.}

\item{normalize}{If \code{TRUE} (default), L2-normalize each row of the output
so cosine similarity equals dot product.  Leave on for retrieval,
clustering, or topic modeling.}

\item{device}{Either \code{"cpu"} (default) or \code{"cuda"} if a GPU is available
and libtorch was installed with CUDA support.}

\item{verbose}{If \code{TRUE}, prints batch progress.  Defaults to interactive
sessions only.}
}
\value{
A numeric matrix with \code{length(texts)} rows and \code{hidden_size}
columns.
}
\description{
Tokenizes, runs the encoder forward pass, mean-pools over tokens (masked
by the attention mask), and optionally L2-normalizes the result.  Batches
inputs for memory efficiency.
}
\examples{
\dontrun{
  enc <- load_hf_bert("sentence-transformers/all-MiniLM-L6-v2")
  emb <- embed_texts(enc, c("First sentence.", "Second sentence."))
  # Cosine similarity = dot product (because rows are L2-normalized)
  emb \%*\% t(emb)
}
}
