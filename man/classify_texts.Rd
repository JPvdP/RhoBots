% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify_texts}
\alias{classify_texts}
\alias{classify_texts.hf_classifier}
\alias{classify_texts.default}
\title{Classify or label texts with a fine-tuned BERT-family model}
\usage{
classify_texts(classifier, texts, ...)

\method{classify_texts}{hf_classifier}(
  classifier,
  texts,
  batch_size = 32L,
  max_length = 512L,
  verbose = FALSE,
  ...
)

\method{classify_texts}{default}(classifier, texts, ...)
}
\arguments{
\item{classifier}{An \code{hf_classifier} from \code{\link[=load_hf_classifier]{load_hf_classifier()}}.}

\item{texts}{A character vector of strings.}

\item{...}{Unused (for future extension).}

\item{batch_size}{Number of texts per forward pass. Default 32.}

\item{max_length}{Maximum token sequence length (including special tokens).
Sequences are truncated to this value. Default 512.}

\item{verbose}{Print batch progress. Default FALSE.}
}
\value{
For sequence tasks: a \code{data.frame} with \code{nrow(texts)} rows.
For NER: a \code{list} of \code{data.frame}s, one per input text.
}
\description{
S3 generic that dispatches on the classifier class returned by
\code{\link[=load_hf_classifier]{load_hf_classifier()}}.
}
\details{
\strong{Sequence classification} (sentiment, topic, …): returns a \code{data.frame}
with columns \code{text}, \code{label}, \code{score}, plus one probability column per
label.  For \code{problem_type = "regression"} the label columns contain raw
numeric scores.  For \code{problem_type = "multi_label_classification"} each
label column contains a sigmoid probability and there is no single \code{label}
or \code{score} column.

\strong{Token classification / NER}: returns a named \code{list} of \code{data.frame}s,
one per input text, each with columns \code{token}, \code{label}, \code{score}.  Special
tokens (\verb{[CLS]}, \verb{[SEP]}, padding) are automatically excluded.
}
\examples{
\dontrun{
  clf  <- load_hf_classifier("cardiffnlp/twitter-xlm-roberta-base-sentiment")
  res  <- classify_texts(clf, c("I love this!", "Terrible experience."))
  res$label   # c("positive", "negative")
  res$score   # highest class probability

  # VAD regression
  clf <- load_hf_classifier("RobroKools/vad-bert")
  classify_texts(clf, c("I am ecstatic!"))
  # returns: text | valence | arousal | dominance

  # NER
  clf <- load_hf_classifier("dslim/bert-base-NER")
  result <- classify_texts(clf, c("Marie Curie was born in Warsaw."))
  result[[1]]   # token | label | score
}
}
