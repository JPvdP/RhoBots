% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo.R
\name{rhobots_demo}
\alias{rhobots_demo}
\title{Run a quick Rhobots demo using classic novels from Project Gutenberg}
\usage{
rhobots_demo(n_per_book = 120L, device = "cpu", seed = 42L, verbose = TRUE)
}
\arguments{
\item{n_per_book}{Maximum paragraphs to sample per book (default 120).
Lower values are faster; higher values give richer, more stable topics.}

\item{device}{Device for the encoder: \code{"cpu"} (default), \code{"mps"} (Apple
Silicon), or \code{"cuda"} (NVIDIA GPU).}

\item{seed}{Integer random seed for UMAP and sampling (default 42).}

\item{verbose}{Print progress messages (default \code{TRUE}).}
}
\value{
Invisibly, a list with elements \code{fit}, \code{embeddings}, \code{encoder},
and \code{texts} (a data frame with columns \code{text} and \code{title}), so you can
continue exploring after the demo.
}
\description{
Downloads a selection of classic novels, splits them into paragraphs, and
runs the full BERTopic pipeline: embed → UMAP → HDBSCAN → c-TF-IDF.
Prints a topic summary and shows two interactive plots: a 2-D topic map
and a bar chart of top terms per topic.
}
\details{
The encoder used is \code{sentence-transformers/all-MiniLM-L6-v2} (~22 MB),
which is downloaded once and cached by \pkg{hfhub}.  Subsequent runs skip
the download.
}
