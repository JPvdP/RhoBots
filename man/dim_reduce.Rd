% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{dim_reduce}
\alias{dim_reduce}
\title{Fit a dimensionality-reduction model and return the reduced matrix}
\usage{
dim_reduce(model, X, seed = 42L, verbose = FALSE)
}
\arguments{
\item{model}{A dimensionality-reduction model object.}

\item{X}{Numeric matrix to reduce.}

\item{seed}{Random seed (default 42).}

\item{verbose}{Print progress (default \code{FALSE}).}
}
\value{
A list with \code{$embedding} (reduced matrix) and \code{$model}
(the fitted model, for use with \code{\link{dim_project}}).
}
\description{
Fit a dimensionality-reduction model and return the reduced matrix
}
\examples{
\dontrun{
  m   <- umap_reduction(n_neighbors = 5L, n_components = 2L)
  enc <- load_hf_bert("sentence-transformers/all-MiniLM-L6-v2")
  emb <- embed_texts(enc, c("first doc", "second doc", "third doc"))
  res <- dim_reduce(m, emb)
}
}
