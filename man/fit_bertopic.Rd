% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_model.R
\name{fit_bertopic}
\alias{fit_bertopic}
\title{Fit a BERTopic-style topic model}
\usage{
fit_bertopic(
  encoder = NULL,
  docs,
  embeddings = NULL,
  dim_reduction_model = NULL,
  cluster_model = NULL,
  representation_model = NULL,
  umap_n_neighbors = 15,
  umap_n_components = 5,
  umap_min_dist = 0,
  umap_metric = "cosine",
  hdbscan_min_pts = 10,
  hdbscan_method = c("eom", "leaf"),
  top_n_terms = 10,
  language = "english",
  stopwords = NULL,
  extra_stopwords = NULL,
  ngram_range = c(1L, 1L),
  reduce_frequent_words = FALSE,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{encoder}{A loaded encoder, as returned by \code{\link[=load_hf_bert]{load_hf_bert()}}.}

\item{docs}{Character vector of documents.}

\item{umap_n_neighbors}{UMAP \code{n_neighbors} parameter (default 15).}

\item{umap_n_components}{Reduced dimensionality for clustering (default 5).}

\item{umap_min_dist}{UMAP \code{min_dist} (default 0).}

\item{umap_metric}{UMAP distance metric (default \code{"cosine"}).}

\item{hdbscan_min_pts}{HDBSCAN \code{minPts} (default 10).}

\item{top_n_terms}{Number of terms per topic in the output (default 10).}

\item{stopwords}{Character vector of words to drop before c-TF-IDF.}

\item{seed}{Random seed for reproducibility (default 42).}

\item{verbose}{Whether to print progress messages (default TRUE).}
}
\value{
A list (class \code{bertopic_fit}) with elements:
\code{embeddings}, \code{reduced}, \code{layout2d}, \code{clusters}, \code{topic_terms},
\code{hdbscan}, \code{docs}.
}
\description{
Runs the four-stage BERTopic pipeline:
\enumerate{
\item Embed documents with the supplied encoder.
\item Reduce embedding dimensionality with UMAP.
\item Cluster the reduced space with HDBSCAN.
\item Extract per-topic terms with c-TF-IDF.
}
}
\details{
Also computes a separate 2-D UMAP projection of the same embeddings, for
visualization.

This implementation will be extended over time to mirror more of the
Python BERTopic package's capabilities (custom vectorizers, topic
reduction, representation models, dynamic topic modeling).
}
\examples{
\dontrun{
  enc <- load_hf_bert("pritamdeka/S-Scibert-snli-multinli-stsb")
  fit <- fit_bertopic(enc, docs = my_abstracts)
  print_topics(fit)
}
}
