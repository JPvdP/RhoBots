% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pos_representation.R
\name{pos_dtm}
\alias{pos_dtm}
\title{Build a POS-filtered document-term matrix}
\usage{
pos_dtm(
  docs,
  pos = c("NOUN", "PROPN"),
  patterns = NULL,
  lemmatize = TRUE,
  language = "english",
  model_dir = NULL,
  min_df = 2L,
  max_df_frac = 0.95,
  extra_stopwords = character(0L),
  verbose = TRUE
)
}
\arguments{
\item{docs}{Character vector of documents.}

\item{pos}{UPOS tags to retain.  Default \code{c("NOUN","PROPN")}.}

\item{patterns}{Optional list of UPOS sequences for multi-word phrases.}

\item{lemmatize}{Use lemmatised forms (default \code{TRUE}).}

\item{language}{udpipe language name (default \code{"english"}).}

\item{model_dir}{Directory for the cached language model.}

\item{min_df}{Minimum document frequency (default 2).}

\item{max_df_frac}{Maximum document-frequency fraction (default 0.95).}

\item{extra_stopwords}{Character vector of additional words to exclude.}

\item{verbose}{Print progress messages (default \code{TRUE}).}
}
\value{
A sparse \code{dgCMatrix}: documents x POS-filtered vocabulary.
}
\description{
Annotates each document in \code{docs} with a udpipe language model, keeps
only tokens whose UPOS tag is in \code{pos} (and/or phrases matching
\code{patterns}), then builds a sparse document-term matrix from the
surviving tokens.  The result is compatible with \code{\link{c_tf_idf}}.
}
\examples{
\dontrun{
  docs <- c("The neural network learns features.",
            "Gradient descent optimizes weights.",
            "Transformers use attention mechanisms.")
  pos_dtm(docs, pos = c("NOUN", "VERB"))
}
}
\seealso{
\code{\link{pos_representation}}, \code{\link{build_dtm}}
}
