% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{load_hf_classifier}
\alias{load_hf_classifier}
\title{Load a fine-tuned BERT-family classifier from HuggingFace}
\usage{
load_hf_classifier(repo_id, weights_path = NULL, prefix = "")
}
\arguments{
\item{repo_id}{HuggingFace repo ID, e.g. \code{"cardiffnlp/twitter-xlm-roberta-base-sentiment"}
or \code{"RobroKools/vad-bert"}.}

\item{weights_path}{Optional path to a local weights file (\code{.safetensors} or
\code{.bin}).  Overrides Hub weight discovery; config and tokenizer still come
from \code{repo_id}.}

\item{prefix}{Optional string prepended to every text before tokenization
(useful for instruction-tuned classifiers).  Default \code{""}.}
}
\value{
An \code{hf_classifier} list with elements \code{model}, \code{tokenizer}, \code{config},
\code{id2label}, \code{problem_type}, \code{task}, \code{num_labels}, \code{repo_id}, \code{prefix}.
}
\description{
Downloads \code{config.json}, model weights, and a tokenizer from the HuggingFace
Hub and returns an \code{hf_classifier} object.  Supports sequence classification
(sentiment, topic, intent), multi-label classification, regression (e.g. VAD
emotion scores), and token classification (NER, POS tagging).
}
\details{
The task type and output format are auto-detected from \code{config.json}:
\itemize{
\item \code{architectures} containing \code{"ForTokenClassification"} -> NER mode
\item \code{problem_type = "regression"} -> return raw numeric scores
\item \code{problem_type = "multi_label_classification"} -> sigmoid per label
\item otherwise -> softmax single-label classification
}

Supported backbone architectures: the same as \code{\link[=load_hf_bert]{load_hf_bert()}}  --  BERT,
RoBERTa, XLM-RoBERTa, CamemBERT, DistilBERT (backbone only), MPNet.
}
\examples{
\dontrun{
  # Sentiment analysis (single-label, 3 classes)
  clf <- load_hf_classifier("cardiffnlp/twitter-xlm-roberta-base-sentiment")
  classify_texts(clf, c("I love this!", "Terrible experience."))

  # VAD regression (valence, arousal, dominance)
  clf <- load_hf_classifier("RobroKools/vad-bert")
  classify_texts(clf, c("I am ecstatic!", "Feeling nervous about the exam."))

  # Named entity recognition
  clf <- load_hf_classifier("dslim/bert-base-NER")
  classify_texts(clf, c("Albert Einstein was born in Ulm, Germany."))
}
}
