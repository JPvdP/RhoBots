% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{cluster_docs}
\alias{cluster_docs}
\title{Fit a clustering model and return cluster labels}
\usage{
cluster_docs(model, X, seed = 42L)
}
\arguments{
\item{model}{A clustering model object.}

\item{X}{Numeric matrix to cluster.}

\item{seed}{Random seed (default 42).}
}
\value{
A list with \code{$labels} (integer vector; \code{-1} = noise,
\code{0, 1, 2, ...} = cluster IDs) and \code{$model} (the fitted
model).
}
\description{
Fit a clustering model and return cluster labels
}
\examples{
\dontrun{
  m   <- hdbscan_clustering(min_pts = 3L)
  enc <- load_hf_bert("sentence-transformers/all-MiniLM-L6-v2")
  emb <- embed_texts(enc, c("cats and dogs", "machine learning",
                             "pets and animals", "neural networks"))
  res <- cluster_docs(m, emb)
  res$labels
}
}
