% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_model.R
\name{build_dtm}
\alias{build_dtm}
\title{Build a sparse document-term matrix from a character vector}
\usage{
build_dtm(
  docs,
  min_df = 2,
  max_df_frac = 0.95,
  stopwords = character(),
  ngram_range = c(1L, 1L)
)
}
\arguments{
\item{docs}{Character vector of documents.}

\item{min_df}{Minimum document frequency: terms appearing in fewer than
this many documents are dropped.}

\item{max_df_frac}{Maximum document-frequency fraction: terms appearing
in more than this fraction of documents are dropped.}

\item{stopwords}{Character vector of tokens to remove before n-gram
construction.}

\item{ngram_range}{Integer vector of length 2, e.g. \code{c(1, 2)} to
include both unigrams and bigrams.  Default \code{c(1, 1)} (unigrams
only).}
}
\value{
A sparse \code{dgCMatrix} of dimensions \code{length(docs)} x
vocab size, with \code{colnames} set to the vocabulary.
}
\description{
Tokenizes each document (lowercase, split on non-alphanumeric), removes
stopwords, optionally generates n-grams, then applies document-frequency
filtering.  Returns a sparse matrix suitable for the c-TF-IDF step.
}
