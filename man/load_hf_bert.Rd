% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_model.R
\name{load_hf_bert}
\alias{load_hf_bert}
\title{Load a BERT-family model from HuggingFace for use in R}
\usage{
load_hf_bert(repo_id, weights_path = NULL, prefix = "")
}
\arguments{
\item{repo_id}{A HuggingFace repo ID, e.g.
\code{"sentence-transformers/all-MiniLM-L6-v2"} or
\code{"NetworkIsLife/SciBert_Cased_DAFS"}.}

\item{weights_path}{Optional path to a local weights file (\code{.safetensors}
or \code{.bin}).  Useful when (a) the upstream model lacks safetensors and
you've downloaded a converted version from a PR branch, (b) you've
produced a local conversion, or (c) you want to pin to a specific local
file rather than the latest on the Hub.  When supplied, this overrides
the Hub weight discovery; \code{config.json} and the tokenizer are still
pulled from \code{repo_id}.}
}
\value{
A list with three elements:
\describe{
\item{\code{model}}{the loaded \code{bert_model} nn_module}
\item{\code{tokenizer}}{a tokenizer object exposing \code{encode_batch()}}
\item{\code{config}}{the architecture config as a list}
}
}
\description{
Downloads a model's config, weights, and tokenizer from the HuggingFace
Hub and returns an "encoder" object that can be passed to \code{\link[=embed_texts]{embed_texts()}}
or \code{\link[=fit_bertopic]{fit_bertopic()}}.  Works for any model with a standard BERT
architecture: vanilla BERT, MiniLM, MPNet, SciBERT, BioBERT, ClinicalBERT,
BERT-for-Patents, FinBERT, LegalBERT, the sentence-transformers built on
top of these, and so on.
}
\details{
The function tries to use modern formats when available and falls back
transparently when they aren't:
\itemize{
\item \strong{Weights}: \code{model.safetensors} is preferred (device-agnostic, safe,
fast).  If absent, \code{pytorch_model.bin} is read via R-torch's pickle
loader.  If the model has only \code{pytorch_model.bin} and that file was
saved on a CUDA device, the load will fail — convert the upstream
model to safetensors first (see HuggingFace's \code{safetensors/convert}
Space) and pass the result via \code{weights_path}.
\item \strong{Tokenizer}: \code{tokenizer.json} (HuggingFace fast tokenizer via the
\code{tok} package) is preferred.  If absent, falls back to
\code{\link[=make_wordpiece_tokenizer]{make_wordpiece_tokenizer()}} reading \code{vocab.txt} (requires the
\code{wordpiece} package).
}
}
\examples{
\dontrun{
  enc <- load_hf_bert("sentence-transformers/all-MiniLM-L6-v2")
  emb <- embed_texts(enc, c("Hello world.", "Another sentence."))

  # BGE model — prefix at load time, applied automatically on every embed call
  enc <- load_hf_bert("BAAI/bge-base-en-v1.5",
                       prefix = "Represent this sentence: ")

  # E5 model — passage prefix for documents
  enc <- load_hf_bert("intfloat/e5-base-v2", prefix = "passage: ")
  # Override per-call for query-side embedding:
  query_emb <- embed_texts(enc, queries, prefix = "query: ")

  # With a custom weights file (e.g. safetensors from an unmerged PR)
  enc <- load_hf_bert("pritamdeka/S-Scibert-snli-multinli-stsb",
                      weights_path = "/path/to/local/model.safetensors")
}
}
